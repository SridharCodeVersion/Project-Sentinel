Act as a Principal Full-Stack Engineer and Core Security Systems Architect. We need to implement our "PROJECT SENTINEL: Unified Command Center" screen into the designated backend and frontend files to establish a live-operational full-stack prototype running over local network architectures.

### 1. THE LIVE BACKEND ARCHITECTURE (server.js)
- Construct a lightweight Express.js server in the backend workspace.
- Implement an in-memory data store tracking the 5 primary Indian cybercrime hotspots (NCR Delhi, Mewat, Jamtara, Mumbai, Bengaluru) and a dynamic data array for the High-Value Intelligence Ledger.
- Create an active background interval system (setInterval) on the server that runs every 5 seconds to programmatically fluctuate real-time operational metrics (e.g., dynamically varying Active Vectors by +/- 3, updating active VoIP Node counts, and generating brand new randomized law enforcement log objects containing realistic Indian bank names, locations, and unique alphanumeric serial combinations).
- Expose two clean REST API endpoints:
  - GET /api/hotspots: Returns the complete up-to-the-second JSON array of all city metrics.
  - GET /api/ledger: Returns the latest 10 localized live security alerts generated by the server pipeline.

### 2. THE LIVE-FETCHING FRONTEND ARCHITECTURE (src/App.jsx)
- Update our main frontend dashboard layout to completely eliminate hardcoded client-side states. 
- Initialize empty state arrays for 'hotspots' and 'ledgerLogs'. 
- Implement a master React useEffect pipeline that triggers an asynchronous fetch workflow immediately upon component mounting. Use window polling hooks (setInterval executing every 4 seconds) to perform live HTTP GET requests to /api/hotspots and /api/ledger.
- Implement a clear, visual pulsing "Live Network Sync" heartbeat indicator node next to the MHA header to explicitly show evaluation judges that the system is pulling fresh data payloads over live ports.
- Synchronize our interactive SVG India map layout with this incoming stream: clicking an SVG city node filters the actively fetched 'hotspots' state payload to instantly re-populate the right-hand Briefing Panel with real-time variable modifications.
- Transition the bottom Intelligence Ledger smoothly so new alert packets dropping from the live server prepend onto the screen layout.

### 3. PRODUCTION STYLE GUIDELINES
- Retain our premium matte slate-obsidian design theme (#0A0F1D) with crisp, thin structural borders (#1F2937). Ensure all telemetry streams use the absolute Indian Standard Time (IST) derived natively from the server cluster.
- Ensure all asynchronous error states (.catch()) are gracefully managed, displaying micro-loading spinners if connectivity drops rather than breaking layout rules. Provide clean, fully expanded, production-ready code with zero shortcuts.