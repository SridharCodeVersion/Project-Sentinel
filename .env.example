# Optional — required only for Replit Auth / user sessions
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sentinel

# Local dev ports (defaults shown)
# API_PORT=8080
# WEB_PORT=18756
# API_PROXY_TARGET=http://localhost:8080
